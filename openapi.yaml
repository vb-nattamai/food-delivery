# Auto-generated stub by AgentReady — fill in your actual paths and schemas.
# Run: npx @redocly/cli lint openapi.yaml   to validate.
openapi: 3.1.0
info:
  title: Food Delivery Platform
  description: "A polyglot microservices platform for food delivery that handles order\
    \ management, restaurant inventory and menu items, driver tracking and dispatch,\
    \ payment processing via Stripe, and customer notifications via SMS and email.\n\
    \n**Auto-generated stub** \u2014 fill in paths, schemas, and security.\nStack:\
    \ typescript / Spring Boot"
  version: 0.1.0
paths:
  /health:
    get:
      summary: Health check
      operationId: health_check
      responses:
        '200':
          description: Service is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
components:
  schemas: {}
  securitySchemes: {}
